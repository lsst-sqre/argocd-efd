{{- if and (ne .Values.env "ncsa-int") (ne .Values.env "ncsa-stable") -}}
# Argo CD is already deployed on ncsa-int and ncsa-stable
apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: tls-certs
  namespace: argocd
spec:
  path: secret/dm/square/efd/{{ .Values.env }}/tls-certs
  type: Opaque
{{- end -}}
